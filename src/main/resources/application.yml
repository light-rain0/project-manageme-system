#spring.application.name=project-manageme-system

spring:
  datasource:
    url: "jdbc:mysql:///project_db"
    username: root
    password: 123456

mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl